# Image-Steganography-using-LSB
 This project is  an image steganography system in C using Least Significant Bit (LSB) encoding to securely hide and retrieve secret data within BMP image files. The application supports both encoding and decoding through command-line arguments, ensuring data integrity by embedding metadata.
